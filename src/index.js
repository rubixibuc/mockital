const mock = require("./mock");
const inspect = require("./inspect");
const when = require("./when");
const reset = require("./reset");
const resetWhen = require("./resetWhen");

module.exports = {
  mock,
  inspect,
  when,
  reset,
  resetWhen
};
